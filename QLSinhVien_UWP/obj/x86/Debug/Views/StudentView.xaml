<Page
    x:Class="QLSinhVien_UWP.Views.StudentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QLSinhVien_UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <controls:DockPanel>
        <controls:DockPanel controls:DockPanel.Dock="Top">
            <StackPanel controls:DockPanel.Dock="Left">
                <PersonPicture HorizontalAlignment="Center" VerticalAlignment="Center"></PersonPicture>
            </StackPanel>

            <VariableSizedWrapGrid x:ConnectionId='5' Grid.Row="0" Orientation="Horizontal" controls:DockPanel.Dock="Top"
                                   x:Name="StackTextbox">
                <VariableSizedWrapGrid.Resources>
                    <Style TargetType="TextBox">
                        <Setter Property="Margin" Value="5"></Setter>
                        <Setter Property="Width" Value="200"></Setter>
                    </Style>

                    <Style TargetType="ComboBox">
                        <Setter Property="Margin" Value="5"></Setter>
                        <Setter Property="Width" Value="200"></Setter>
                    </Style>

                    <Style TargetType="CalendarDatePicker">
                        <Setter Property="Margin" Value="5"></Setter>
                        <Setter Property="Width" Value="200
                                "></Setter>
                    </Style>
                </VariableSizedWrapGrid.Resources>
                <TextBox x:ConnectionId='13' x:Name="TxbID" Header="MSHS" PlaceholderText="Mã số học sinh" 
                                                          ></TextBox>
                <TextBox x:ConnectionId='14' x:Name="Txbname" Header="Họ và tên" PlaceholderText="Họ và tên"
                                                          ></TextBox>
                <TextBox x:ConnectionId='15' x:Name="TxbAddress" Header="Địa chỉ" PlaceholderText="Địa chỉ"></TextBox>
                <TextBox x:ConnectionId='16' x:Name="TxbEmail" Header="Email" PlaceholderText="Email"></TextBox>

                <ComboBox x:ConnectionId='17' x:Name="CbbGender" Header="Giới tính" PlaceholderText="Giới tính" Margin="5"
                          >
                </ComboBox>

                <CalendarDatePicker x:ConnectionId='18' x:Name="DpBirthDate" Header="Ngày sinh" Width="200" Margin="5"></CalendarDatePicker>

                <ComboBox x:ConnectionId='19' x:Name="CbbClass" Header="Lớp"></ComboBox>
                <Button x:ConnectionId='20' Content="Thêm lớp" Margin="0 22 0 0" x:Name="BtnAddClass"
                                                 ></Button>
                <ComboBox x:ConnectionId='21' x:Name="CbbClassType" Header="Khối"                                                 ></ComboBox>

                <CalendarDatePicker x:ConnectionId='22' x:Name="DpStartDate" Header="Năm học"></CalendarDatePicker>
                <CalendarDatePicker x:ConnectionId='23' x:Name="DpEndDate" Header="Năm học"></CalendarDatePicker>
            </VariableSizedWrapGrid>

            <StackPanel x:ConnectionId='6' Orientation="Horizontal" HorizontalAlignment="Right" Margin="5"
                        x:Name="StackButton">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="5"></Setter>
                        <Setter Property="MinWidth" Value="125"></Setter>
                        <Setter Property="Height" Value="50"></Setter>
                        <Setter Property="FontSize" Value="15"></Setter>
                    </Style>
                </StackPanel.Resources>

                <Button x:ConnectionId='7' x:Name="BtnSkip"                      >
                    <StackPanel Orientation="Horizontal">
                        <SymbolIcon Symbol="Clear"></SymbolIcon>
                        <TextBlock Text="Bỏ qua" FontSize="20"></TextBlock>
                    </StackPanel>
                </Button>

                <Button x:ConnectionId='8' x:Name="BtnSave"                      >
                    <StackPanel Orientation="Horizontal">
                        <SymbolIcon Symbol="Save"></SymbolIcon>
                        <TextBlock Text="Lưu" FontSize="20"></TextBlock>
                    </StackPanel>
                </Button>
                <Button x:ConnectionId='9' x:Name="BtnAdd"                     >
                    <StackPanel Orientation="Horizontal">
                        <SymbolIcon Symbol="Add"></SymbolIcon>
                        <TextBlock Text="Thêm" FontSize="20"></TextBlock>
                    </StackPanel>
                </Button>

                <Button x:ConnectionId='10' x:Name="BtnEdit"                      >
                    <StackPanel Orientation="Horizontal">
                        <SymbolIcon Symbol="Edit"></SymbolIcon>
                        <TextBlock Text="Sửa" FontSize="20"></TextBlock>
                    </StackPanel>
                </Button>

                <Button x:ConnectionId='11' x:Name="BtnDelete"                        >
                    <StackPanel Orientation="Horizontal">
                        <SymbolIcon Symbol="Delete"></SymbolIcon>
                        <TextBlock Text="Xóa" FontSize="20"></TextBlock>
                    </StackPanel>
                </Button>

                <Button x:ConnectionId='12' x:Name="BtnRefresh">
                    <StackPanel Orientation="Horizontal">
                        <SymbolIcon Symbol="Refresh"></SymbolIcon>
                        <TextBlock Text="Làm mới" FontSize="20"></TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>
        </controls:DockPanel>

        <StackPanel controls:DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBox x:ConnectionId='3' x:Name="FilterByName" Width="150" Header="Tên" Margin="8"
	     HorizontalAlignment="Left"                              />
            <TextBox x:ConnectionId='4' x:Name="FilterByID" Width="150" Header="MSHS" Margin="8"
	     HorizontalAlignment="Left"                              />
        </StackPanel>

        <controls:DataGrid x:ConnectionId='2' ItemsSource="{Binding ListStudent}" Grid.Row="1" HorizontalScrollBarVisibility="Auto"
                           x:Name="gvStudent" AutoGenerateColumns="False"                                              
                           IsReadOnly="True" CanUserSortColumns="True">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="MSHS" Binding="{Binding ID}"></controls:DataGridTextColumn>
                <controls:DataGridTextColumn Header="Họ tên" Binding="{Binding Name}"></controls:DataGridTextColumn>
                <controls:DataGridTextColumn Header="Giới tính" Binding="{Binding Gender}"></controls:DataGridTextColumn>
                <controls:DataGridTextColumn Header="Ngày sinh" Binding="{Binding BirthDate}"></controls:DataGridTextColumn>
                <controls:DataGridTextColumn Header="Lớp" Binding="{Binding Class.Name}"></controls:DataGridTextColumn>
                <controls:DataGridTextColumn Header="Khối" Binding="{Binding ClassType.Name}"></controls:DataGridTextColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </controls:DockPanel>
</Page>

